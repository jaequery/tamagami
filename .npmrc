# Expo / React Native + pnpm: Metro's resolver can't traverse pnpm's default
# symlinked node_modules, so install a flat (hoisted) layout like npm/yarn.
# https://docs.expo.dev/guides/monorepos / Expo "Using pnpm" guidance.
node-linker=hoisted
